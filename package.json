{
  "name": "search-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon start.js",
    "start": "pm2 start start.js",
    "stop": "pm2 delete start.js",
    "build": "npm install && mkdir dist && xcopy public dist\\public /E /I && xcopy views dist\\views /E /I && xcopy src dist\\src /E /I && copy server.js dist\\ && copy routes.js dist\\ && copy *.js dist\\",
    "serve": "node start.js"
  },
  "author": "Sandro A Costa",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "search-api": "file:",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
