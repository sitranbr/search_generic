{
  "name": "search-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon start.js",
    "start": "pm2 start start.js",
    "stop": "pm2 delete start.js",
    "build": "npm install && mkdir -p dist && cp -r public dist/ && cp -r views dist/ && cp -r src dist/ && cp server.js dist/ && cp routes.js dist/ && cp *.js dist/",
    "serve": "node start.js"
  },
  "author": "Sandro A Costa",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "search-api": "file:",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
